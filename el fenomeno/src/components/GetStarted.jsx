import styles from "../style";

const GetStarted = () => (
  <div className={`${styles.flexCenter} box mt-20 cursor-pointer`}>
    <p className="font-poppins font-normal text-[24px] leading[23px] text-white">Learn More</p>
  </div>
);

export default GetStarted